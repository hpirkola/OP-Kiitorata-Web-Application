{
  "name": "my-app",
  "private": true,
  "workspaces": ["frontend", "backend", "cdk"],
  "scripts": {
    "deploy": "npm --workspace frontend run build && cd cdk && cdk deploy",
    "deploy:destroy-mode": "npm --workspace frontend run build && cd cdk && cdk deploy -c removalPolicy=destroy",
    "build": "npm --workspace frontend run build && npm --workspace backend run build",
    "destroy": "cd cdk && cdk destroy"
  }
}